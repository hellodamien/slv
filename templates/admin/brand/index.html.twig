{% extends 'base.html.twig' %}

{% block title %}Marques{% endblock %}

{% block body %}
    <div class="d-flex flex-grow-1 flex-column m-4">
        <div class="d-flex flex-column flex-grow-1 justify-content-center p-4 text-black">
            <h1 class="align-self-center">Marques</h1>
            <h2 class="align-self-center fs-4">Les marques de véhicules disponibles</h2>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Marque</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for brand in brands %}
                    <tr>
                        <td><a href="{{ path('admin_brand_show', {'id': brand.id}) }}">{{ brand.name }}</a></td>
                        <td>
                            <a href="{{ path('admin_brand_edit', {'id': brand.id}) }}" class="btn btn-primary">Modifier</a>
                            <a href="{{ path('admin_brand_delete', {'id': brand.id}) }}" class="btn btn-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="{{ path('admin_brand_new') }}" class="align-self-center col-3 btn btn-success">Ajouter une marque</a>
    </div>
{% endblock %}