{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <div class="d-flex flex-grow-1 flex-column m-4">
        <div class="d-flex flex-column flex-grow-1 justify-content-center p-4 text-black">
            <h1 class="align-self-center">Statut</h1>
            <h2 class="align-self-center fs-4">Les statuts de véhicules disponibles</h2>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Type</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for status in statuses %}
                    <tr>
                        <td><a href="{{ path('admin_status_show', {'id': status.id}) }}">{{ status.name }}</a></td>
                        <td>
                            <a href="{{ path('admin_status_edit', {'id': status.id}) }}" class="btn btn-primary">Modifier</a>
                            <a href="{{ path('admin_status_delete', {'id': status.id}) }}" class="btn btn-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="{{ path('admin_status_new') }}" class="align-self-center col-3 btn btn-success">Ajouter un statut</a>
    </div>
{% endblock %}